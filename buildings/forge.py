import pygame

from settings import cellSize

class Forge():
    def __init__(self):
        self.x = 0
        self.y = 0
        self.rotation = 0

    def show(self, window):
        pygame.draw.rect(window, )

    def place(self, x, y, rotation):
        pass